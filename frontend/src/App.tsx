import React from 'react';
import PlayGround from './components/PlayGround';
import './App.scss';

const App: React.FC = () => {
    return (
        <div className="app">
            <PlayGround/>
        </div>
    );
};

export default App;
